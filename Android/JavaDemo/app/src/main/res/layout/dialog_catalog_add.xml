<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="280dp"
    android:layout_height="wrap_content"
    android:gravity="center">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="添加目录"
        android:textColor="@color/black"
        android:textSize="@dimen/title"
        android:background="@color/bt_login"
        android:padding="8dp"
        android:gravity="center"/>
    <EditText
        android:id="@+id/et_catalog_dialog_tag"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="章节"
        android:inputType="number"
        android:padding="10dp"/>
    <EditText
        android:id="@+id/et_catalog_dialog_title"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="目录名"
        android:padding="10dp"/>
    <LinearLayout
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="15dp">

        <Button
            android:id="@+id/bt_dialog_submit"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="确定"
            android:textColor="@color/bg_text"/>

        <Button
            android:id="@+id/bt_dialog_cancel"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="取消"
            android:textColor="@color/bg_text"/>
    </LinearLayout>
</LinearLayout>